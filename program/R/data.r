library(sampling)
library(outliers)
library(resample)
#library(sampledata)
# library(resamplingdata)

library(vegan)
library(agricolae)

AllOTU=read.table('./16S_otutable_total.csv',header=T, sep=",")

